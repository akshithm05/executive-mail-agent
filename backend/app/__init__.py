"""AI Executive Email Assistant — backend application package.

The single source of truth for the application version lives here and is
re-used by the FastAPI app metadata, the ``/version`` endpoint, and packaging.
"""

__version__ = "0.1.0"
